# Depth Report Notes

- `all.json` is generated by `npm run qa:scss:v2`.
- Each entry records:
  - `maxBlockDepth`
  - `maxSelectorDepth`
  - repeated selectors
  - variable definition counts
- A file is blocking if `maxSelectorDepth > 3`.
